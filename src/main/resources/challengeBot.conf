{
  db = {
    include "db.json"
  },
  triggers = {
    include "triggers.json"
  },
  mail = {
    include "mail.json"
  },
  slack = {
    include "slack.json"
  },
  strava = {
    include "strava.json"
  }
}
